<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="dev.bwaim.loteria.initializers">

    <application>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="dev.bwaim.loteria.initializers.CoilInitializer"
                android:value="androidx.startup" />

            <meta-data
                android:name="dev.bwaim.loteria.initializers.StrictModeInitializer"
                android:value="androidx.startup" />

            <meta-data
                android:name="dev.bwaim.loteria.initializers.TimberInitializer"
                android:value="androidx.startup" />

            <meta-data
                android:name="dev.bwaim.loteria.initializers.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

    </application>

</manifest>
